<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/brewed_nav"
    app:startDestination="@id/displayProductsActivity">

    <activity
        android:id="@+id/displayProductsActivity"
        android:name="com.example.brewedapp.DisplayProductsActivity"
        android:label="activity_display_products"
        tools:layout="@layout/activity_display_products" />
    <activity
        android:id="@+id/accountActivity"
        android:name="com.example.brewedapp.AccountActivity"
        android:label="activity_account"
        tools:layout="@layout/activity_account" />
    <activity
        android:id="@+id/cartActivity"
        android:name="com.example.brewedapp.CartActivity"
        android:label="activity_cart"
        tools:layout="@layout/activity_cart" />
</navigation>